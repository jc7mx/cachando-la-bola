---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';

// Importamos el componente de Imagen optimizada de Astro
import { Image } from 'astro:assets';
// Importamos el logo (el archivo debe estar en src/assets/)
import blogLogo from '../assets/logo-deportes-cachando-la-bola.webp';
---

<!doctype html>
<html lang="es">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />
		<main>
			<div class="intro-container">
				<Image 
					src={blogLogo} 
					alt="Logo oficial de Cachando la Bola" 
					class="hero-logo"
					width={250} 
					density={[1.5, 2]}
				/>
				
				<h1>¡Bienvenidos a Cachando la Bola!</h1>
				
				<p>
					¡Qué onda! Soy <strong>JC</strong>. 
					Este es mi rincón personal donde la ingeniería de software se toma un descanso 
					y la pasión por los deportes toma el control. 
				</p>
				
				<p>
					Aquí no encontrarás análisis de expertos de televisión, pero sí la emoción genuina 
					de un fanático que vive cada <strong>Super Bowl</strong>, cada final del <strong>Mundial</strong> de fútbol
					y cada <strong>Juegos Olímpicos</strong> como si fuera el último.
				</p>

				<div class="action-buttons">
					<p>¿Quieres saber cómo empezó esta locura?</p>
					<a href="/blog/bienvenidos-a-cachando-la-bola" class="button-primary">
						Leer el primer post 🚀
					</a>
				</div>
			</div>
		</main>
		<Footer />
	</body>
</html>

<style>
	/* Estilos para que se vea centrado y profesional */
	.intro-container {
		text-align: center;
		padding: 2em 0;
		max-width: 720px;
		margin: 0 auto;
	}
	
	.hero-logo {
		display: block;
		margin: 0 auto 2em auto; /* Centra la imagen y da espacio abajo */
		border-radius: 50%; /* Hace la imagen redonda (si el diseño lo permite) */
		box-shadow: 0 4px 20px rgba(0,0,0,0.15); /* Sombra suave para darle profundidad */
		transition: transform 0.3s ease;
	}

	.hero-logo:hover {
		transform: scale(1.02); /* Efecto sutil al pasar el mouse sobre el logo */
	}

	h1 {
		font-size: 2.5em;
		margin-bottom: 0.5em;
		color: rgb(var(--black));
	}

	.action-buttons {
		margin-top: 2.5em;
		padding-top: 1.5em;
		border-top: 1px solid rgba(var(--gray), 0.2);
	}

	.button-primary {
		display: inline-block;
		background-color: var(--accent); /* Usa el color de acento de tu tema */
		color: white;
		padding: 12px 24px;
		border-radius: 25px;
		text-decoration: none;
		font-weight: bold;
		box-shadow: 0 2px 5px rgba(0,0,0,0.2);
		transition: all 0.2s ease;
	}

	.button-primary:hover {
		transform: translateY(-2px); /* El botón "salta" un poquito al pasar el mouse */
		box-shadow: 0 4px 8px rgba(0,0,0,0.3);
		text-decoration: none;
	}
</style>